interface UserButtonProps {}

const UserButtons: React.FC<UserButtonProps> = ({}) => {
  return (
    <div>
      <button>Hit</button>
      <button>Stay</button>
    </div>
  );
};

export default UserButtons;
